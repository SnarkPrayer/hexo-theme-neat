<% if (post.tags && post.tags.length){ %>
	<div class="article-tag">
		<svg class="icon"><use xlink:href="#icon-tag" /></svg> <%- list_tags(post.tags, { show_count: false, style: 'link' }) %>
  </div>
<% } %>
